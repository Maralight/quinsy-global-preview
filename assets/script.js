
const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.main-nav');if(menu&&nav)menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});
document.querySelectorAll('form.demo-form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();alert('Preview only: the quotation form is not yet connected.')}));
document.querySelectorAll('[data-preview-action="whatsapp"]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();alert('The direct WhatsApp link will be activated after the official WhatsApp number is confirmed. The full number is intentionally excluded from this public preview.')}));
document.querySelectorAll('[data-preview-action="social"]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();alert('The official social-media link will be connected after the client provides the approved profile URL.')}));
const progress=document.createElement('div');progress.className='scroll-progress';document.body.appendChild(progress);const update=()=>{const a=document.documentElement.scrollHeight-innerHeight;progress.style.width=(a>0?scrollY/a*100:0)+'%'};addEventListener('scroll',update,{passive:true});update();
const targets=document.querySelectorAll('.section-head,.card,.panel,.detail-block,.process-step,.value-card,.case-card,.cta,.stats-grid,.testimonial-shell');targets.forEach(x=>x.classList.add('reveal'));if('IntersectionObserver'in window){const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');o.unobserve(e.target)}}),{threshold:.1});targets.forEach(x=>o.observe(x))}else targets.forEach(x=>x.classList.add('revealed'));
document.querySelectorAll('.click-card,.btn').forEach(x=>x.addEventListener('pointerdown',()=>{x.classList.remove('is-pressed');void x.offsetWidth;x.classList.add('is-pressed')}));
const slides=[...document.querySelectorAll('.testimonial')];let i=0;function show(n){if(!slides.length)return;slides.forEach(s=>s.classList.remove('active'));i=(n+slides.length)%slides.length;slides[i].classList.add('active')};document.querySelector('.carousel-control.next')?.addEventListener('click',()=>show(i+1));document.querySelector('.carousel-control.prev')?.addEventListener('click',()=>show(i-1));if(slides.length)setInterval(()=>show(i+1),6500);
